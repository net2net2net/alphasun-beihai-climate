# AlphaSun · 北海极端气候全景系统 — v7.0.3 界面调整总览（共 10 项）

> 本地运行地址：**http://localhost:8765**（已运行，改完即时生效）
> 启动方式：`node server.js`（位于 `app/` 目录，纯 Node 零依赖，端口 8765）

## 本次（v7.0.3）完成的调整（共 10 项）

| # | 需求 | 落地位置 | 实现要点 |
|---|------|----------|----------|
| 1 | 地理区域 → 左列首位 | `index.html` / `#stationPicker` | 左栏第一项，便于先选站点 |
| 2 | 极端天气告警 → 右列首位（标红） | `#alertCard` | 右栏第一，标题 `.danger-h` 红色告警样式 |
| 3 | 24h 逐时曲线 → 实时天气下 | `#chartCard` | 紧跟实时天气卡，趋势联动 |
| 4 | 7日/15日预报 → 告警后、天文前 | `#forecastCard` / `#forecast15Card` | 右栏顺序：告警→7日→15日→天文 |
| 5 | 实时天气「变化趋势」 | `app.js` `buildWeatherTrend()` | 1h 后降水/温度升降/风力变化，彩色 chip |
| 6 | 海洋·浪高/海温 + 潮汐/水位可视化 | `renderMarine` / `renderTides` + `tideSVG()` | 浪高条形+浪向箭头+图标；潮汐 SVG 曲线+当前水位≥警戒高亮 |
| 7 | 天文卡：太阳位置图 + 真实月相图 | `sunAltAz()` / `sunDiagramSVG()` / `moonPhaseSVG()` | 按当下数据绘制太阳弧线光点 + 按 moon_phase 渲染明暗终结线 |
| 8 | 霞光/天文景观按北海可见性筛选 | `meteorPeakAlt()` / `RADIANT_DEC` | 流星雨按北海纬度(21.48°N)估算峰值仰角，不可见则标注不展示 |
| 9 | 地理区域选择后展示「区域概况」 | `regionShapeSVG()` / `regionBlock()` | 实时天气卡新增：覆盖面积(km²)+覆盖人口(万)+真实行政区轮廓缩略图（DataV 边界简化，5 个区域） |
| 10 | 点击任意模块标题 → 中央放大展示 | `openFocus()` / `closeFocus()` / `renderChartOn()` | 点击面板标题在页面中央弹层放大该板块与数据；地图/曲线特殊处理（移动 Leaflet 容器 / 重绘 Chart），其余面板克隆 HTML 放大 |

## 关键修复：本地编辑即时生效
- 原 `serveStatic` 为「内联优先」，本地 `node server.js` 会服务陈旧内联资源 → 改前端不生效。
- 改为 **「磁盘优先、内联兜底」**：先读 `public/` 真实文件，缺失再回退内联 base64。
- 效果：本地改 `public/` 下任意文件，刷新即见，无需重建 exe；exe 仍走内联保证单文件可用。

## 验证结果（curl 实测）
- `/js/app.js` 含 5 个新函数（sunAltAz / buildWeatherTrend / moonPhaseSVG / tideSVG / meteorPeakAlt），12 命中
- `/css/styles.css` 含 6 个新类（rt-trend / astro-sun / wave-fill / tide-warn / glow-loc / ev-vis），9 命中
- `/` 布局 ID 顺序正确：stationPicker 左首 → alertCard 右首 → chartCard 在实时天气下 → forecastCard 在 astroCard 前

## 交付与提交
- 已重建 `app/AlphaSun.exe`（87MB，SEA 单文件，内联与源码一致）
- git commit `2380445`（master）：八项调整全量入库

## 待办（需用户操作）
- **GitHub 上传仍阻塞**：当前 gh 登录为细粒度 token（`github_pat_`），无建仓权限。
  - 请在 github.com/new 手动建 **Public 空仓库** `alphasun-beihai-climate`（勿初始化 README/LICENSE/.gitignore）。
  - 建好后我执行 `git remote add origin ... && git push -u origin master`。
  - 若细粒度 token 无 Contents 写权限导致 push 失败，需改用 `ghp_` Classic PAT。
